{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cat \"$HOME/.claude/plugins/config/my-assistant/profile.md\" 2>/dev/null || echo 'assistant: no profile found. Run /assistant:setup to create one. The plugin still works with pasted content.'"
          }
        ]
      }
    ]
  }
}
