## 2026-05-25

- Initial public spec and multi-workspace layout
- Shared skills: `/done`, `/standup`, `/weekly-review`, `/todo-add`, `/todo-review`
- Publishable `workspaces/example/` with fictional persona
- `scripts/install.sh` replaces Makefile-based install
