fixture_id: cal-05-mixed-week
violations_expected:
  - type: buffer
    count_min: 2
  - type: prep
    count_min: 1
  - type: follow-up
    count_min: 1
proposals_expected:
  min_count: 3
  block_types: [buffer, prep, follow-up]
  must_not_contain: [booked, "created event", "I've scheduled"]
focus_defence_level: moderate
queue_items_min: 3
approval_language_checks:
  must_include: [manually, propose]
  must_not_include: [auto-book, "scheduled for you"]
