fixture_id: conn-notes-paste
category: notes
placeholder: "~~notes"
smoke_command: "/assistant:meeting follow-up"
standalone_pass:
  must_surface:
    - decisions or action items
    - follow-up draft or recap
  must_not:
    - send
    - book
    - silent memory write
draft_only: false
connector_gap_behaviour:
  when_missing: "notes gap and continues with paste"
approval_frame_checks:
  optional: true
injection_safe: false
